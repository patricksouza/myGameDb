* Para adicionar algum pacote, use:

ng add nome_do_pacote


-g -> generate
-c -> create

* Para gerar um novo componente, use:
ng g c nome_do_componente

* Para gerar um novo serviço, use:
ng g s http